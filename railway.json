{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install --no-cache -r requirements.txt && uvicorn app.main:app --reload"
  }
}
